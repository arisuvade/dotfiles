set $mod Mod4
set $alt Mod1

# polybar
exec_always --no-startup-id $HOME/.config/polybar/launch.sh

# picom
exec_always picom

# font for window titles.
font pango:Roboto Medium 10

exec --no-startup-id dex --autostart --environment i3
exec --no-startup-id nm-applet

# reload and restart
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart

# adjust audio
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status

# adjust brightness
bindsym XF86MonBrightnessUp exec light -A 5
bindsym XF86MonBrightnessDown exec light -U 5

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

# terminal
bindsym $mod+Return exec --no-startup-id alacritty

# apps
bindsym $alt+b exec brave -disk-cache-dir=Documents --disk-cache-size=104857600
bindsym $alt+c exec google-chrome-stable --new-window classroom.google.com
bindsym $alt+v exec code
bindsym $alt+f exec --no-startup-id thunar
bindsym $alt+l exec libreoffice

# screenshot
bindsym Print exec xfce4-screenshooter -f -s /home/aries/Pictures/Screenshots/Screenshot_"$(date +%Y-%m-%d_%H-%M-%S)".png

# powermenu
bindsym Delete exec --no-startup-id bash /home/aries/.config/rofi/powermenu/type-1/powermenu.sh

# wifimenu
bindsym Insert exec bash /home/aries/.config/rofi/powermenu/type-1/wifimenu.sh

# kill window
bindsym $mod+q kill

# rofi
bindsym $mod+d exec --no-startup-id "rofi -show drun"

# change focus window
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# resize window (you can also use the mouse for that)
mode "" {
        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 1 px or 1 ppt
        bindsym Down resize grow height 1 px or 1 ppt
        bindsym Up resize shrink height 1 px or 1 ppt
        bindsym Right resize grow width 1 px or 1 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}
bindsym $mod+r mode ""

# display resolution
exec_always xrandr --output eDP-1 --mode 1592x892_60.00
 
# display wallpaper
exec_always feh --bg-fill /home/aries/Pictures/Wallpapers/arch.jpg
#exec_always feh --bg-fill /home/aries/Pictures/Wallpapers/ai2.jpg

# pointer speed
exec_always xinput --set-prop 10  'libinput Accel Speed' -0.95

# disable touchpad
#exec_always synclient TouchpadOff=1
exec_always ~/Documents/scripts/automate_touchpad.sh

# pulseaudio echo cancellation
exec_always pulseaudio -k

# remove title bars
# new_window 1pixel
default_border pixel 3

# window gaps
hide_edge_borders smart
gaps inner 5
gaps outer 0
smart_gaps on

# frame color
client.focused		#00BCD4 #000000 #DCDCDC #00BCD4 #00BCD4 #00BCD4
